# =============================================================================
# PadosiAgent Django — Python Dependencies
# Equivalent to Laravel's composer.json
# =============================================================================

# --- Core Framework ---
Django>=5.1,<6.1
djangorestframework>=3.15
django-environ>=0.11           # .env file loading (like Laravel's DotEnv)
django-cors-headers>=4.3       # CORS support
django-filter>=24.0            # DRF filtering

# --- Database ---
mysqlclient>=2.2               # MySQL connector (same as Laravel's PDO MySQL)

# --- Future modules (install when needed) ---
razorpay>=1.4                # Payment gateway (Module 6)
# firebase-admin>=6.0          # FCM push notifications (Module 9)
# django-allauth>=0.60         # Google OAuth (Module 8)
Pillow>=10.0                 # Image processing
cloudinary>=1.36             # Image CDN (Module 8)
requests>=2.31               # HTTP client for Brevo API
geoip2>=4.8                  # IP geolocation for threat monitoring
